/**
 * \struct nce_check_coro_args
 * \brief Options for ::nce_check_coro_spawn.
 */
struct nce_check_coro_args {
#if defined(DOXYGEN)
	/**
	 * (private)
	 *
	 * Dummy field because no options.
	 */
	int dummy;
#endif
};
