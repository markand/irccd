/**
 * \struct nce_prepare_coro_args
 * \brief Options for ::nce_prepare_coro_spawn.
 */
struct nce_prepare_coro_args {
#if defined(DOXYGEN)
	/**
	 * (private)
	 *
	 * Dummy field because no options.
	 */
	int dummy;
#endif
};
