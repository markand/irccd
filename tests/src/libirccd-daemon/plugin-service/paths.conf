[paths]
cache = "/var/cache"
data = "/share"
config = "/etc"

[paths.p1]
cache = "/var/super-cache"

[paths.p2]
config = "/super-etc"

[paths.p3]
data = "/super-share"

[paths.all]
cache = "/var/super-cache"
config = "/super-etc"
data = "/super-share"
